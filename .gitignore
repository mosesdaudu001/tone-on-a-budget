__pycache__/
*.pyc
.ipynb_checkpoints/
# runtime artifacts (see assets/README.md) — keep weights/calibration out of git
assets/*.pt
assets/*.json
*.wav
# ...except the 5 poster QR "Listen" demo clips, which MUST ship in the repo:
!docs/audio/zs_hoe.wav
!docs/audio/ft_hoe.wav
!docs/audio/zs_vehicle.wav
!docs/audio/ft_vehicle.wav
!docs/audio/ft_vehicle_flat.wav
.DS_Store

# private: S3->HF uploader stays out of the public repo
notebooks/10_upload_artifacts_to_hf.ipynb

# private: blind-pilot answer keys + reviewer responses must NEVER reach the public repo
# (committing keymap*.json would deanonymise the listening test)
keymap*.json
*answers*.csv
*responses*.csv

# private: contributor voice recordings (native-speaker reference clips) — do NOT publish
# without explicit consent for publication. Keep raw personal audio out of the public repo.
*.opus
*.m4a
